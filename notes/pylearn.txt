https://www.freecodecamp.org/news/how-to-parse-xml-in-python-without-using-external-libraries/

datatypes 
    (immutable)
    1. int   = without decimals, for example, 10 or -5.
    2. float = with a decimal point, for example, 3.14 or -0.5.
    3. str   = immutable - sequence of characters, enclosed in quotes, 
    4. bool  = either True or False.
    5. None  = null or empty value
    6. range = sequence of numbers, 
        

    collection of items
    7. tuple = immutable - () parentheses, 
        for example, (1, 2, 3) or ('apple', 'banana', 'cherry').
    8. list  = mutable - [] square brackets, 
        for example, [1, 2, 3] or ['apple', 'banana', 'cherry'].
    7. set   = mutable - unique items - {} curly braces, 
        for example, {1, 2, 3} or {'apple', 'banana', 'cherry'}.
    8. dict  = mutable - key-value pairs - {} curly braces, 
        for example, {'name': 'John', 'age': 30}.
    

The built-in isinstance() function 
lets you check if a variable matches a specific data type
- isinstance('Hello world', str) # True

all datatypes are treated as obj

string
    - multiline
    - len(s)
    - indexing
    - concatenation
str() - provides string version of any obj without changing original value
    - interpolation = f-string
    - slicing

num--ops
    - +, -, *, /, 
    - % = modulo results in remainder 
    - // = floor division results in integer 
    - ** = exponentiation = pow(x, y)
    - fncs = float(), int(), round(), abs(), max(), min(), sum(), prod(), 

    - Augumented Assignnments = concise way todo numops
    - = +=, -=, *=, /=, %=, //=, **=

bools and conditional
    compare-ops = ==, !=, <, >, <=, >=  for comparing values
        - always gives boolean results 
        - if-elif-else = for branching multiple conditional statements  
        - > = greater than
        - < = less than

        Inbuilt bool() function. It explicitly converts a value to its boolean equivalent 
        and returns True for truthy values and False for falsy values
        - truthy = non-zero numbers, non-empty strings, non-empty collections, True
        - falsy = 0, 0.0, '', [], {}, None, False

        - nested if-esle will be in-effecient and hard to read
    bool-ops (logical) - used in complex conditional statements
        - and = && , (TTT, TFF, FTF, FFT)
        - || = or, (short-circuit operator) (TTF , FFF, TFT, FTT)
        - ! = not, unary = converts bool value to opposite 

fncs
    - print() = print not store value
    - return() = return value to caller
    - param : arg = key:value
    SCOPE
    - LEGB rule - local, enclosing, global, built-in
    - enclosing - fnc nested fnc
        - non-local - allows modifying a variable from an enclosing function
    - global

in labs
    - my code - logic understainding was wrong
    - forgot old thing like - isinstance, tuple()
    - 
    - Both of these give the exact same answer, 
      but the "1 -" version is a "pro" shortcut used by programmers and mathematicians:
        - The Long Way: price - (price * (discount / 100))
            (Full Price minus the Discount Amount)
        - The "1 -" Shortcut: price * (1 - (discount / 100))
            (Price multiplied by the "Leftover" Percentage)

    - GTK abt GUARD Clause  
        - checking errs at top - protect fnc from bad data
        - eg -> if price <= 0 : return ("error)
    - GTK abt isinstance is used for type-checking
    - practice dry run (writing notes)


28-03-26

basic Sequence type
    - list, tuples, range
    - use list while working with dynamic data
    - use tuple while working with immutable data

list [] - mutable
    - index can used to update value of list
    - nested list = list [1][2]
    - del, in 
    - unpacking = decounstructing a list
    - slicing = : (slice operator)
        eg. [2:5], [1::2]
    - * = collect any remaining elements from a list

    methods
    - append - will add type and value as it is - at the end of list
    - extend - add values to the list individually
    - insert(index, value)
    - remove()
    - pop()- take value from end of the list
    - clear - remove all items from list
    - sort() - modify orginal list
    - sorted() - new list with sort - not modifying original
        - key = eg key=len

    error
    - indexerr 
    - valueerr

tuple () - immutable
    
    methods
    - count()
    - index(value,start,stop)
    - sorted

loop
    - for
    - while - untill condition is False
    - break-continue

range
    - start, stop, step
    - decrementing - can used to print in descending order
    - The range() function is a very handy way to generate a sequence of integers in Python. 
    - Once you get the hang of it, you'll probably find yourself using it a lot in your Python programs.

enumerate and zip fncs
    - enumerate ([], start) 
        - keep track of index
    - zip - work in paralell on mutiple list - resutls in tuple

 

lambda - anoynomous inline fnc to work with high-order fnc
    - do not use as variable
    - avoid creating lambda fncs that are difficult to read or unnecessarily complicated

num-pattern
    - same logic can have differ approach


290326


dict and sets

dict = { key:value } - aloows fast lookup using keys
    - pizza['name'] = 'Margherita'
    - .get() = dictionary.get(key, default) = results in view obj(read-only dict)
    - .keys()
    - .values 
    - .items - results in list of tuples
    - clear
    - .pop(key)
    - .update


    loops on dict
    - for using ,key, .value, .items
    - enumerte on dict - results in tuples


300326

sets = { 1, 2, 3 } - cant have duplicate - unique and unorderd
    - .add
    - .remove or .discard (differ is - remove give keyerr if not found - discard doesn't)
    - .issubset or .issuperset
    - .isdisjoint - they don't have any elements in common
    - "|" - union operator - returns a new set with all the elements from both sets
    - "&" - intersection op - returns a new set with only the elements that the sets have in common
    - "-" - returns a new set with the elements of the first set that are not in the other sets
    - "^" - returns a new set with the elements that are either in the first or the second set, but not both.
    - |= &= -= ^= -  These operators automatically assign the resulting set to the first set in the expression:

modules
    -math, random, re for regex, datetime
    - import , from(specific fncs for module)
    - as = alias

020426
    - practice med valid fnc
    - ** operator can be used to unpack the elements in a dict {}
    - practically used regex module (re) 
        - GTK abt re.search('value\special-sequence', "key", re.IGNORECASE) = Find it anywhere
        - GTK abt re.fullmatch = The whole thing must match

        Function          What it does	                                 Result for "cat" in "big cat"
        re.search()	   -> Scans the whole string for the pattern.     -> Match (finds it at the end)
        re.fullmatch() -> Checks if the entire string is the pattern. -> None (it's not an exact match)

        - special-sequence = \d = decimal digit
        - quantifier = way to tell the computer, "Look for this character, but look for it this many times."
                Instead of typing aaaaa, you can just type a{5}.
                The 3 Most Common Symbols:
                    ? (The Optional One): Means "0 or 1 time."
                        Example: cars? matches both car and cars. (The 's' is optional).
                    * (The "Whatever" One): Means "0 or more times."
                        Example: hi* matches h, hi, hii, hiii, etc.
                    + (The "At Least One" One): Means "1 or more times."
        - need work on weak area
            - list-comprehensions = new_list = [expression for item in iterable if condition]
            - with for loop
            - logic building - problem understainding 
  
060426
    - list can be made 2 way - list() constructor or lit comprehensions
    - list comprehensions 
        = new_list = [expression for item in iterable if condition]
    high-order fnc
        - filter( fnc, iterable)
        - map()
    - sum(iterable, start)

070426
    - https://youtube.com/shorts/jaPR7B5jrPQ?si=TZofVYAOAsFk204j
    - usecase - webdev & automations - python

110426
    - need practice on dict 
    - need work on weak area
            - list-comprehensions = new_list = [expression for item in iterable if condition]
            - with for loop
            - logic building - problem understainding 
  - type() is example of explicit data conversion
  - division math-ops is implicit data conversion
  
  

150426 - brilliant
    - linear algebra 
        - patterns
            - linear
                - describing patterns - how big it is - 3n,4n - 2n represents the number of squares on the nth step,
                - Building expressions
                - linear expressions
            - growing
        - constant terms
            - n + 6 = 6 is constant term
        

160426
    - label:"good first issue" language:python state:open

170426
    - interactive debugging - pdb module   
    -  pdb.set_trace()

    -  print() statements for quick checks, 
    - pdb for interactive exploration, and 
    - IDE debuggers for visual inspection.

    - exception handling
    -  try, except, else, and finally 
    - finally: Runs no matter what—whether or not an exception occurred. Useful for clean-up tasks like closing files or releasing resources.

180426
    - i found 1blue3brown yt channel
    - linear algebra 
        - vectors = ordered list = [x,y]
        - operations
            - vector addition
            - scaling of vectors = multiplication = scalar multiplication
        - xy = coordinate System
            - basis vector
                - i hat = unit vector of x
                - j hat = unit vector of y
        - span is set of all the linear comboniations of v and w = av + bw = a and b can vary
        - the basis of vector space is a set of linearly indopendent vectors that span the full space
    - linear transformations(fnc) 
        - vector input - vector output
        2 properties
            - all lines are lines without getting curved
            - origin should stay fixed
        - gird lines remain parallel and evenly spaced

220426
    - psuedocode learnings
        p1 - to get squares
            - to result into list there are 3 solutions
                - using append method - std
                eg - take empty list and into the append the result

                -  '' list comprehensions - best approach - faster then lambda 
                eg - result = [x**2 for x in nums]

                - map - hardway
                eg - result = (list(map(int,nums))) = output -> [1,2,3,4,5]
                - result = (list(map(lambda x : x**2, nums)))

        p2 - to get divisible by 10
            - in list comprehensions
                - expression / math-ops are declared before loop
                eg result = [x**2 for x in num]

                - if statements after loop
                eg result = [x for x in num if (x % 10) == 0] # divisible 10

        p3 - str list
            - can use split or list-com

        p4 - nested list using list com
            - parathesis () matter alot - i missed
                - divisible
                - nested loop 
            eg vectors = [(x,y) for x in range(1,3) for y in range(1,4)]

        p5 - Print students with marks > 75 - dict-com
            - use curly braces { }
            eg result = {name: marks for name, marks in students.items if marks > 75 }

230426
    - err-handling
        - Syntax-err
        - Name-err - variable not defined
        - Type-err
        - Index-err
        - Attribute-err - method/prop not exist for that obj type
    - exception-handling
        - try-except - except = safety net
        - else-finally - extends try-except
        - finally - runs even if err occurs
    - exception obj = used in code "as e" using as keyword
        - raise - creating err
    - exception signaling
        -   Detect -> Signal -> Route -> Resolve
        eg The code notices a problem (e.g., balance is $0). Use raise MyCustomSignal().The program stops the current function and looks for a try/except block. The except block performs the specific fix for that signal
        - from keyword is used to chain the exceptions


        p6 - list to set - remove duplicates
            - using set() automatically removes duplicate values
            - set will always unordered - no specific usecase to sort - if needed import sortedset 

        p7 - two sum
            - Approach - complement search
            S1 - hashmap = empty dict - fast even on mil-items on search

    - Note : list act similar to array but are different
            - array used in numpy
            - array is mem-effecient - need to import numpy to use
            - exetremely fast on large data then list
            - data is of same type

020526
    - 2-shape patterns
        -   2n+3n = 5n
030526  - comparing expressions = Subtracting expressions is another way to combine them.

    - var[key] = value = dictkey assignnment
     
050526
    - follow brilliant py playlist topics

110526
    - get job in a month 
    - by learning enough for cracking interview
    - build project to showcase while learning process

    - starting project CLI-todo
    - requirement.txt - is created during dev,before share/deploy, while cloning
    - 2 way - pip freeze and using tool called pipreqs

    - can use touch to create multiple files in a folder
    eg - touch m1.py m2.py m3.py 

FCC - OOPS
    - practical use of is to 

    - class = blueprint to create objs with
    - within class can add intializer, with attributes and methods
        - att = var in class
        - method = fnc in class
    
    - ordering matters in attributes
        non-default value = required inputs
        default value = pre-set value even input not given

note - initializer is automatically declared while using @dataclass
        same is used for type hints - if not then can use __init__

120526
    - there are two types of attributes - 
        - Instance - unique to each obj 
        - class - belong to class itself and shared by all Instances(objs)
        eg  class Dog:
                species = "French Bulldog" # Class attribute

                def __init__(self, name):
                    self.name = name # Instance attribute
    - dunder methods = double underscore = special fncs

    - Safety: In Python, None is a singleton. 
    Using is or is not is faster and safer than using ==


    - handling Attributes dynamically (https://www.freecodecamp.org/learn/python-v9/lecture-classes-and-objects/how-to-handle-object-attributes-dynamically)
        - getattr() - to add
        - setattr() - to create/update-existing-one
        - hasattr() - to find
        - delattr() - to remove
        - dir() - is used loop over obj using for loop

Note: dynamic handling of attributes is practically needed in
        eg like multiuser support, db backend, async ops, realtime-updtes, 
            plugin-systems, rest-api/web-app, concurrent access
    
    - decorator = automatic code generators

060626
    - found mit 2016 - even old but still great
    - setup - focus one thing - python - odoo company
    - tasks at claude
    - html flask TBD
    - 2 achieved - review pending 
    - task 3 test and task 4 is remaining - pullover tommorow
    - GTL 
        - T1 warn-up on list comp syntax = [var-with-numops for var in list if condition ]
        - T2 warn-up on dict-comp = {key: val for key, val in dict if concise}
            - GTK abt dict-comp - from operator import itemgetter
            - itemgetter is better then lambda
            - GTK abt 2 approach - sorted and max(BP)
            - came up with approach
            - sum logic was given by ai - like which components to set - recall memory gap
        - T3 - ai assited to identify - gap
            - if task_id == task_id: # Before (WRONG)
            - if task.id == task_id: # After (RIGHT)
            - return f"found it: {task}" # Before String, not usable
            - return task # After Actual object, can use



070626
    - setup strict standard for better code in vs code
    - done PO - tast 3 test and task 4
    - GTL
        - GTK abt test case BPs 
        - T4 was abt removing duplicates from list while keeping order
            - GTL abt dual structure approach - set and list - extra memory needed
            - modern way is to use dict
                - why - dict maintain INSERTION ORDER
                - no extra needed
            - logic was assisted by ai - not got to think much - understanding gap
            - after trying wrong approach - ai assisted improvements
            - O(n) - LIST lookup is SLOW
            - Loop 100k times = 100k × O(n) = O(n²) 
            - O(1) - SET lookup is FAST
    - today task is to review and improve
    
080626
    - if we do int("3.5") it will give error
    - Instead do int(float("3.5"))
    - given odoo test got clarity
    - GTL abt MVC and flask setup

090626
    - abandon flask for django
    - move towards stricter odoo approach
    - Strict OOPs
    - Django use variant called MVT similar to MVC
    - Django enforces oops and mvc concepts by default
    - python -m pip install Django
    - workshop came in handy from 3yrs ago

130626
    - user input - kms-to-miles
    - try to understand basic oops syntax
    - try to implement input to arcade pass today
    - started sql at sololearn
        - select col from table
        - sql code send reqs known as queries
        - col = field, row = record
        - header - top row/record of table - contain name of fields
    -  make calc / converter app
        - from whatever learned yet

140626
    - first solve 50 basic program in py
        - for logic building and dev problem solving skills
    - given 4 days time to complete task 
    py example
    - x=y=z="hello" - legal
    - x,y,z = red,blue,yellow- legal
    - list = [1,2,3] 
        - a,b,c = list 
        print(a) # result to a=1
    - there is also type called complex
        - x = 5
        - x = complex(x)
    - if we x[3] to output 
        - it will give work on str as list and return char on index 3
    - if want to find a word in str - use in
    - str.strip -  string without any whitespace at the beginning or the end.
    - str.replace("A","B")

    - fstr 
        - can use for math ops
        - if-else statements
    
    - Comp ops
        - # Variables given
            a , b = 5, 3

            # Evaluates the comparison directly and assigns the boolean
            result = a > b

            # Uses the 'not' operator to invert the boolean directly
            inverse_result = not result

    - <<= bitwise left shift operator
        
        - do Comment the Intent
        - use fast multiplication
        - use in low-lvl sys 
        - do not
            - for Simple Multiplication
            - 

    - walrus operator ( := )
        - print(x:=3) - print at same time save value to x 
        - BP - always use () parentheses for precedence 
             - either output T/F resulting wrong result
        - keep var name meaningful and clear
        - were to use
           - to check API responses or heavy DB queries
           - In List Comprehensions (Filtering and Reusing Data)
           - When modifying elements in a list, 
                - you often want to filter out elements based on an expensive function call, 
                - but still keep the transformed result.
                
                import math
                
                numbers = [4, 9, 16, 25]

                # Without Walrus, you would have to run math.sqrt(x) twice: 
                # once for the 'if' filter and once for the output list.

                # With Walrus: math.sqrt(x) is computed once, saved to 'root', and reused
                square_roots = [root for x in numbers if (root := math.sqrt(x)) > 3]

                print(square_roots)  # Outputs: [4.0, 5.0]
        - do not
            - Replace Simple Assignments
            - Chain Multiple Walruses
            -  if it Conceals Bugs

150626
    - else never hold another condition - its elif
    - while loop uses counter var to avoid infinite loop - used when iterations are unknown
    - for is used when iterations are known
    - str concate in list items 
    - for sq nums - used list-comp 
    - list.insert(item,index)
    - normal-oops  - https://youtu.be/-pEs-Bss8Wc?si=Q3ovtYVbsdbZfYdd
        - classes are complex ds/obj
            - have fncs that decide behaviour of classes
            - is blueprint  of a ds
        - instance is implementation of that ds on data give
        - attr = var 
        - instance attr can be accessed outside of class - kind of local var
        eg pet_1.name
        - class attr - use upon class - kind of global var  
        eg dog.alias # pet
        - fncs/method without self param - kind of act class attr
            - they use decorator for this 
        - trying to decode email class using psuedocode
            - GTK abt obj relationship
                - composition - Has-A
                - dependency - Use-A


    strict-oops
        - pivate-vars/dunder attr are for strict-encapsulation
        - cannot be directly accessed outside of class
        - so we use props called  
            - getter methods - to read data safely
            eg pet_1.get_name()
            - setter methods - to modify data safely

160726
    - model = class represent concept = blueprint = complex DS
    - Object = actual thing created from that blueprint that holds data
    - A class describes what an object is.
    - A method describes what that object can do.
    - How to describe a method properly
        When you look at a method, ask these 3 questions:
            What does this method do?
            What object does it work on?
            What changes or result does it produce?
    - input - params are - temp inputs
    - state - attr are - permanent in mem state of instance
    - inheritance
        - Inherit -> base class as param to another class
        eg softwaredev(employee)
        - extend -> base clase instance attr used by another class using super
        eg : super().__init__(param1,param2)
        
170726
    - override -> 
    - polymorphism = many forms
    - if print a fnc/method outside of class - it results with type hint too

180726
    - Think of a universal Play Button (▶️):
        - If you press it on a CD Player, it spins a disc.
        - If you press it on a Smartphone, it streams a digital file.
        - If you press it on a Video Game, it unpauses the engine.
    - practice

    











        


         
